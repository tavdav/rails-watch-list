<h3>Add a new bookmark for <%= @list.name %></h3>

<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :comment %>
  <%= f.association :movie %>
  <%= f.submit class: 'btn btn-primary' %>
<% end %>
